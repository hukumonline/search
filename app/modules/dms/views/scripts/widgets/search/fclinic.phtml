<? if (count($this->aData) > 0) : ?>
<div class="content-box corners">
    <header>
        <h3><img src="<?=$this->cdn('images');?>/information.png" alt="" />
        <? if ($this->getLanguage() == "id") { ?>
        Berdasarkan Kategori Klinik
        <? } else { ?>
        Clinic Category
        <? } ?>
        </h3>
    </header>
    <section>
        <form id="frmFcClinic" name="frmFcClinic" method="post" action="<?=$this->baseUrl();?>/<?=$this->getLanguage();?>/dms/search/result">
        <table class="no-style full">
            <tbody>
                <? for($i=0; $i<count($this->aData);$i++) : ?>
                <tr>
                    <td><a href="<?=$this->aData[$i][0];?>" id="fcClinic"><abbr><?=$this->getCatalogAttribute($this->aData[$i][0],'fixedTitle');?></abbr></a></td>
                    <td class="ta-right"><?=$this->aData[$i][1];?></td>
                </tr>
                <? endfor; ?>
            </tbody>
        </table>
        <input type="hidden" id="keyword" name="keyword" />
        <input type="hidden" id="ct" name="category" value="klinik" />
        </form>
    </section>
</div>
<script>
$(document).ready(function(){
    $('#fcClinic').live('click', function(){
        var iId = $(this).attr('href');
        var q = '<?=$this->query.' kategoriklinik:';?>' + iId;
        $("#keyword").val(q);
        $("#frmFcClinic").submit();

        return false;
    });


});
</script>


<? endif; ?>